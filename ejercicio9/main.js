"use strict";

const topics = ['JavaScript', 'Variables', 'funciones', 'condicionales', 'bucles'];

const upperTopics = topics.map(eachTopic => eachTopic.toUpperCase());

upperTopics.reverse();

alert(upperTopics);